#include<bits/stdc++.h>
using namespace std;

class Builder{
    private:
        const string kConfigPath = "./wkconfig";
        const string kMakePath = "./Makefile";
    public:
        void build();
};